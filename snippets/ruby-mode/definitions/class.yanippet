# name: class ... end
# contributor: hitesh <hitesh.jasani@gmail.com>
# key: class
# --
class ${1:`(let ((fn (capitalize (file-name-nondirectory
                                 (file-name-sans-extension
				 (or (buffer-file-name)
				     (buffer-name (current-buffer))))))))
           (cond
             ((s-contains? "_" fn) (s-upper-camel-case fn))
              (t fn)))`}
  $0
end
