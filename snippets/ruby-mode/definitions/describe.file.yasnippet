# name: describe 'file' do ... end
# key: desc
# --
describe "${1:`(replace-regexp-in-string
                    "_spec$" ""
                    (file-name-nondirectory
                     (file-name-sans-extension
                      (or (buffer-file-name)
                          (buffer-name (current-buffer))))))`}" do
  $0
end
